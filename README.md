
  # Orchestrix

  This is a code bundle for Orchestrix. The original project is available at https://www.figma.com/design/BYGDnvwotsw7a7xP9ImxcK/Orchestrix.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  